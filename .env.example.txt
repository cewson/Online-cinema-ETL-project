# PostgreSQL - метаданные Airflow
POSTGRES_AIRFLOW_USER= #Ваш логин для подключения к Postgresql
POSTGRES_AIRFLOW_PASSWORD= #Ваш пароль для подключения к Postgresql
POSTGRES_AIRFLOW_DB= #Имя базы данных

# Warehouse-PostgreSQL - база данных для сбора и хранения данных
POSTGRES_USER= #Ваш логин для подключения к Warehouse-PostgreSQL
POSTGRES_PASSWORD= #Ваш пароль для подключения к Warehouse-PostgreSQL
POSTGRES_DB= #Имя базы данных

# ClickHouse - база данных для построения аналитических витрин
CLICKHOUSE_USER= #Ваш логин для подключения к ClickHouse
CLICKHOUSE_PASSWORD= #Ваш пароль для подключения к ClickHouse
CLICKHOUSE_DB= #База данных для подключения (например, default)

# MinIO - сервер хранения данных
MINIO_ENDPOINT= #Адрес MinIO
MINIO_ROOT_USER= #Ваш логин для подключения к MinIO
MINIO_ROOT_PASSWORD= #Ваш пароль для подключения к MinIO
MINIO_BUCKET= #Название бакета для хранения данных

# Airflow - ПО для оркестрации потоков операций по обработке данных
AIRFLOW_UID= #UID пользователя Airflow
_AIRFLOW_WWW_USER_USERNAME= #Ваш логин для веб-интерфейса Airflow
_AIRFLOW_WWW_USER_PASSWORD= #Ваш пароль для веб-интерфейса Airflow

# Telegram Logger - для настройки telegram-оповещений о работе проекта
TELEGRAM_BOT_TOKEN= #Токен чат-бота
TELEGRAM_CHAT_ID= #ID чат-бота

# Metabase
MB_DB_FILE=/metabase-data/metabase.db

#TMDB API KEY - ключ аунтетификации к APi сервису TMDB
TMDB_API_KEY= #Ваш ключ TMDB

# Airflow — подключения (Admin → Connections или через переменные окружения)
# warehouse_default: PostgreSQL → host warehouse-postgres, port 5432, schema из POSTGRES_DB
# minio_default: Amazon Web Services → Extra: {"endpoint_url": "http://minio:9000"}
#   Login: MINIO_ROOT_USER, Password: MINIO_ROOT_PASSWORD

# Доля невалидных событий от генератора (суммарно ~5% по умолчанию)
RAW_INVALID_DATA_RATE=0.025   # ломают RawEvent → raw.events_invalid на этапе minio→raw
DDS_INVALID_DATA_RATE=0.025   # проходят RawEvent → raw.events_invalid на этапе raw→dds